# Milestone 3 — Transaction List FlatList Performance TODO

- [x] task-1: Refactor `src/app/(tabs)/index.tsx` — remove `ScrollView`, add `FlatList` with `HomeHeader` + `HomeEmpty` sub-components, move transactions query inline
- [x] task-2: Delete `src/features/transaction-list/`
